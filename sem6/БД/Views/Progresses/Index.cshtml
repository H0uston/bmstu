@model IEnumerable<DistanceLearning.Progress>

@{
    ViewData["Title"] = "Index";
}

<h1>Прогресс</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TotalCountTasks)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompletedTasks)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TotalCountTasks)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompletedTasks)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
